[project]
authors = [
  {name = "Jose Monge", email = "info@josemonge.dev"},
]
dependencies = []
description = "Add your description here"
name = "pypm"
readme = "README.md"
requires-python = ">=3.14"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]
