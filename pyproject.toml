[project]
name = "algorithmic-trading-with-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.9",
    "pandas>=3.0.2",
    "pypm",
    "scikit-learn>=1.8.0",
]

[tool.uv.workspace]
members = [
    "src/pypm",
    "sim",
    "listings/chapter_5",
    "listings/chapter_6",
    "listings/chapter_7",
]

[tool.uv.sources]
pypm = { workspace = true }

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
