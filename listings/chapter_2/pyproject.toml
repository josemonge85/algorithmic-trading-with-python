[project]
dependencies = [
  "pypm",
]
description = "Add your description here"
name = "chapter-2"
readme = "README.md"
requires-python = ">=3.14"
version = "0.1.0"

[tool.uv.sources]
pypm = {workspace = true}
