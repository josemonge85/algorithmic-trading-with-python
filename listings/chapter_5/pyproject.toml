[project]
name = "chapter-5"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pypm",
]

[tool.uv.sources]
pypm = { workspace = true }
